name: Qwen3.5 metadata comparison status probe

on:
  push:
    branches: ["experimental"]
    paths:
      - '.github/workflows/qwen35_metadata_compare_status_probe.yaml'
  workflow_dispatch:

permissions:
  contents: write
  actions: read

jobs:
  probe:
    runs-on: ubuntu-latest
    env:
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    steps:
      - uses: actions/checkout@v4
      - name: Capture comparison workflow status
        shell: bash
        run: |
          set -euo pipefail
          gh api "repos/$GITHUB_REPOSITORY/actions/workflows/qwen35_compare_original_vs_mcp240_metadata.yaml/runs?branch=experimental&per_page=5" > "$RUNNER_TEMP/runs.json"
          python - <<'PY' > "$RUNNER_TEMP/probe.json"
          import json, os
          d=json.load(open(os.environ['RUNNER_TEMP']+'/runs.json'))
          out={'schema':'qwen35-metadata-compare-probe.v1','runs':[
              {k:r.get(k) for k in ('id','run_number','status','conclusion','head_sha','created_at','updated_at','html_url')}
              for r in d.get('workflow_runs',[])
          ]}
          print(json.dumps(out,indent=2))
          PY
          CONTENT="$(base64 -w0 "$RUNNER_TEMP/probe.json")"
          API="repos/$GITHUB_REPOSITORY/contents/docs/qwen35_metadata_compare_workflow_probe.json"
          RESPONSE="$(gh api "$API?ref=experimental" 2>/dev/null || true)"
          SHA="$(printf '%s' "$RESPONSE" | python -c 'import json,sys; d=json.load(sys.stdin); print(d.get("sha", ""))' 2>/dev/null || true)"
          if [ -n "$SHA" ]; then
            gh api --method PUT "$API" -f message='Update Qwen3.5 metadata comparison probe' -f branch=experimental -f sha="$SHA" -f content="$CONTENT"
          else
            gh api --method PUT "$API" -f message='Add Qwen3.5 metadata comparison probe' -f branch=experimental -f content="$CONTENT"
          fi
